name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Welcome to OPTIT Lab"
      - name: Print message
        run: echo "My First Github actions work flow executed successfully"
